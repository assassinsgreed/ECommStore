<%= form_for(@business_info) do |f| %>
  <% if @business_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@business_info.errors.count, "error") %> prohibited this business_info from being saved:</h2>

      <ul>
      <% @business_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :about %><br />
    <%= f.text_area :about %>
  </div>
  <div class="field">
    <%= f.label :contact %><br />
    <%= f.text_area :contact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
